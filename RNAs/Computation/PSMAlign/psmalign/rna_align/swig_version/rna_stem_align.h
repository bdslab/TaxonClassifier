#include <vector>

extern std::vector < std::vector<std::string> > align_stem(std::string stem1_base_seq_str, std::string stem2_base_seq_str, std::vector < std::vector < int > > stem1_base_pairs, std::vector < std::vector < int > > stem2_base_pairs, int stem1_upstream_end, int stem1_downstream_start, int stem2_upstream_end, int stem2_downstream_start);
